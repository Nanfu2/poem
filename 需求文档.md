好的，没问题。基于您明确的技术选型（Vue前端 + Supabase服务端），我为您量身定制一份“诗海寻踪”AI赋能诗词赏析平台的MVP版本需求文档。本MVP将聚焦核心价值，快速验证市场。

---

## **诗海寻踪 (Shi Hai Xun Zong) - AI赋能诗词赏析平台 - MVP版本需求文档**

**文档信息**
- **文档版本：** MVP v1.0
- **创建日期：** 2025-08-20
- **文档类型：** MVP需求文档
- **技术栈：** 前端 Vue 3 + Pinia + Vue Router | 服务端 Supabase (PostgreSQL + 实时API + 认证 + 存储)
- **目标读者：** 项目团队、开发人员、测试人员

### **1. MVP目标与范围**

#### **1.1. MVP核心目标**
1.  **技术可行性验证：** 验证Vue与Supabase技术栈在构建内容型AI应用的高效性。
2.  **核心价值验证：** 向早期用户提供“AI智能赏析”这一核心功能，收集用户对AI解读质量的反馈。
3.  **最小化可行产品：** 实现一个具备核心浏览、搜索和AI交互功能的可上线版本，不做社区、个性化推荐等复杂功能。

#### **1.2. MVP核心功能范围**
- **前台用户端：** 诗词浏览、诗词搜索、查看AI赏析、与AI诗友进行问答。
- **后台管理端：** 诗词数据管理（增删改查）、AI密钥配置。
- **不包含的功能（V2考虑）：**
    - 用户注册/登录（MVP期所有用户匿名访问）。
    - 用户收藏、个人中心、学习报告。
    - 复杂的AI智能体架构（仅实现一个核心的“赏析讲解AI”）。
    - 个性化推荐、创作辅助、趣味测试。

### **2. 技术架构与数据模型**

#### **2.1. 架构图**
```
用户浏览器 (Vue 3 App)
    ↓ (HTTP Calls)
Supabase API (作为BaaS后端)
    ├── PostgreSQL (核心数据存储)
    ├── Supabase Auth (预留，MVP暂不启用)
    ├── Edge Functions (AI代理接口)
    └── Storage (预留，存放诗人头像等)
        ↓ (Serverless Function调用)
外部AI服务 (OpenAI GPT-4o/GLM等)
```

#### **2.2. Supabase数据表设计 (核心)**
1.  `poems` (诗词表)
    - `id` (bigint, 主键)
    - `title` (text, 诗词标题)
    - `author` (text, 作者)
    - `dynasty` (text, 朝代)
    - `content` (text, 诗文内容，格式为`\n`分隔的字符串)
    - `background` (text, 可选，创作背景)
    - `created_at` (timestamptz, 创建时间)

2.  `ai_analyses` (AI赏析缓存表) - **用于优化成本和速度**
    - `id` (bigint, 主键)
    - `poem_id` (bigint, 外键，关联`poems.id`)
    - `analysis_content` (text, AI生成的完整赏析内容)
    - `ai_model` (text, 使用的AI模型)
    - `created_at` (timestamptz, 创建时间)
    - `updated_at` (timestamptz, 更新时间)
    - 唯一索引：`(poem_id, ai_model)` 防止对同一首诗重复生成。

3.  `configs` (配置表) - **用于敏感信息管理**
    - `key` (text, 主键，如 `'openai_api_key'`)
    - `value` (text, 配置值)

#### **2.3. Supabase Edge Functions (AI代理)**
由于Supabase的`pgvector`在MVP中非必需，且为了安全隔离AI密钥，我们将创建一个Edge Function作为AI代理。

- **功能：** `generate-poem-analysis`
- **输入：** `{ "poem_id": number, "poem_content": string, "title": string, "author": string }`
- **逻辑：**
    1.  接收前端请求。
    2.  从`configs`表读取AI API密钥。
    3.  构造Prompt（如：“请对古诗《[标题]》由[作者]创作，进行赏析：[内容]”）。
    4.  调用外部AI API（如OpenAI）。
    5.  **（关键）** 将生成的赏析内容**插入/更新**到`ai_analyses`缓存表中。
    6.  将赏析内容返回给前端。
- **好处：** 同一首诗首次请求后，后续请求可直接从`ai_analyses`表读取，极大节省成本并提升速度。

### **3. 前端功能需求 (Vue 3)**

#### **3.1. 页面清单与路由**
1.  **首页 (路由: `/`)**
    - **布局：** 顶部导航栏 + 主体内容区。
    - **功能：**
        - 展示一个醒目的“每日一诗”（可硬编码一首热门诗，如《静夜思》）。
        - 展示一个简单的诗词列表（最新添加的20首，支持分页）。
        - 顶部提供全局搜索框。

2.  **诗词搜索列表页 (路由: `/search?q=关键词`)**
    - **功能：**
        - 接收搜索关键词。
        - 使用Supabase的`postgrest-js`库对`poems`表的`title`, `author`, `content`字段进行**全文搜索**。
        - 展示搜索结果列表（诗名、作者、匹配的内容片段）。

3.  **诗词详情页 (路由: `/poem/:id`) - MVP核心页面**
    - **功能：**
        - 上部：清晰展示诗词的`title`, `author`, `dynasty`, `content`。
        - 下部：**“AI赏析”模块**。
            - 默认状态：显示一个“生成AI赏析”的按钮。
            - 点击按钮后，调用`generate-poem-analysis` Edge Function。
            - 加载中显示动画。
            - 成功后，展示AI生成的赏析内容。
            - **再次访问此页时，优先从`ai_analyses`缓存表读取，直接显示，无需再次生成。**
        - **“问诗友”模块：**
            - 一个简单的文本输入框，用户可以针对这首诗提问（如：“这首诗表达了什么情感？”）。
            - 提问时，将诗词上下文和问题一并发送给Edge Function，由AI解答。

#### **3.2. 组件设计**
- `NavBar.vue`: 包含Logo和搜索框。
- `PoemCard.vue`: 用于在首页列表和搜索结果中展示单首诗的摘要。
- `PoemDetail.vue`: 诗词详情页的主要组件。
- `AIAnalysis.vue`: 封装AI赏析相关的状态（生成中、内容展示）。
- `ChatWithAI.vue`: 一个简单的对话界面，用于“问诗友”功能。

### **4. 后台管理端需求**

- **技术：** 与前台共用一套Vue代码，通过路由守卫控制访问（如`/admin`路径）。
- **功能：**
    1.  **诗词管理：**
        - 列表查看所有诗词。
        - 新增一首诗（表单包含标题、作者、朝代、内容、背景）。
        - 编辑现有诗词。
        - 删除诗词（软删除或硬删除）。
    2.  **系统配置：**
        - 一个简单的表单，用于更新`configs`表中的AI API密钥。**（此页面需做高强度安全保护）**

### **5. MVP成功指标 (KPI)**

1.  **功能完成度：** 100%实现上述功能清单。
2.  **性能：**
    - 诗词列表/搜索页面首次加载时间 < 1.5秒。
    - AI赏析首次生成响应时间 < 5秒（依赖外部AI）。
    - 缓存命中后的赏析加载时间 < 500毫秒。
3.  **AI成本控制：** 通过缓存机制，将重复请求的AI调用成本降至接近零。
4.  **用户反馈：** 收集至少50位种子用户对“AI赏析”质量的正面评价率 > 70%。

### **6. MVP实施计划 (建议2-3周)**

- **第1周：**
    - 搭建Vue 3 + Supabase开发环境。
    - 在Supabase中创建数据库表，并导入至少100首经典诗词数据。
    - 开发前台首页、搜索列表页、诗词详情页的静态部分。
- **第2周：**
    - 开发Supabase Edge Function `generate-poem-analysis`。
    - 前端集成AI赏析和问答功能。
    - 开发后台管理端的诗词CRUD功能。
- **第3周：**
    - 集成测试、UI/UX优化。
    - 部署到Vercel/Netlify（前端）和Supabase（生产环境）。
    - 邀请种子用户进行内测，收集反馈。

---

**总结：** 这份MVP文档以Vue和Supabase为基石，通过精心设计的数据库缓存结构和Edge Function，以最低的成本和最快的速度实现“AI智能赏析”这一核心价值。团队可以此为基础，快速启动开发，并依据MVP的反馈数据规划V2版本的功能（如引入用户系统、pgvector实现语义搜索等）。